
// Function to display a welcome message
function displayWelcomeMessage() {
  console.log("Welcome to my portfolio!");
}

// Function to calculate the total number of projects
function calculateTotalProjects(projects) {
  return projects.length;
}

// Array to store project names
var projects = ["Project 1", "Project 2", "Project 3"];

// Display the welcome message
displayWelcomeMessage();

// Calculate and display the total number of projects
var totalProjects = calculateTotalProjects(projects);
console.log("Total projects: " + totalProjects);
